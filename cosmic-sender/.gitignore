node_modules/
dist/

# release/ is intentionally NOT committed to git — the GitHub Actions Windows
# workflow builds fresh, working .exe files and attaches them to workflow
# artifacts / GitHub Releases.
release/

.env
.env.local
*.log
.DS_Store
